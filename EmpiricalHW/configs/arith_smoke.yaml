# Short run to verify Part 1.2 pipeline (not for submission plots).
out_dir: runs/part1_2_smoke
seed: 0
device: cpu
task: arith
split_seed: 0
p: 97
op: "+"
n_layer: 1
n_head: 4
n_embd: 128
dropout: 0.0
bias: true
block_size: 256
batch_size: 64
max_steps: 400
lr: 0.001
weight_decay: 1.0
betas: [0.9, 0.999]
grad_clip: 1.0
log_every: 50
eval_every: 100
save_every: 400
warmup_steps: 0
loss_answer_only: true
sanity_mask_first_body_tokens: 0
