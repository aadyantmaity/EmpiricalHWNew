# Regenerates `checkpoints/p97_add_1L_restart1_seed101/` (600-step run, same as restart1_quick).
# Run from `code/`: `python train.py --config ../configs/part1_2_p97_add_1L_restart1_deliverable_ckpt.yaml`
out_dir: ../checkpoints/p97_add_1L_restart1_seed101
seed: 101
device: cpu
task: arith
split_seed: 0
p: 97
op: "+"
n_layer: 1
n_head: 4
n_embd: 128
dropout: 0.0
bias: true
block_size: 256
batch_size: 64
max_steps: 600
lr: 0.001
weight_decay: 1.0
betas: [0.9, 0.999]
grad_clip: 1.0
log_every: 50
eval_every: 100
save_every: 600
loss_answer_only: true
